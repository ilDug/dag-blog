import * as React from 'react';
import { useState, useEffect } from 'react';

type Props = {
    id: number
}

const SearchPost: React.FC<Props> = ({ id }) => {
    return (
        <React.Fragment>

        </React.Fragment>
    );
};

export default SearchPost;